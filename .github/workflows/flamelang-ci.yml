name: 🔥 FlameLang CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  FLAMELANG_VERSION: "2.0.0"

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout
      uses: actions/checkout@v4

    - name: 🦀 Setup Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        components: rustfmt, clippy

    - name: 📦 Cache Cargo
      uses: Swatinem/rust-cache@v2

    - name: 🔍 Format Check
      run: cargo fmt --all -- --check

    - name: 🧹 Clippy Lint
      run: cargo clippy --all-targets --all-features -- -D warnings

    - name: 🔨 Build
      run: cargo build --verbose

    - name: 🧪 Test
      run: cargo test --verbose

  # Future: Add these jobs as we build out
  # layer-tests:
  #   name: 5-Layer Pipeline Tests
  #   needs: build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - Layer 1: Linguistic (English → Hebrew)
  #     - Layer 2: Numeric (Unicode → Decimal)
  #     - Layer 3: Wave (c=2πr → Hz)
  #     - Layer 4: DNA (Freq → Codon)
  #     - Layer 5: LLVM IR generation
